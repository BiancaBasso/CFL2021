// realizar un programa que devuelva la potencia de un numero 
ler read =require('readline-sync');
funcion calcularPotencia(base,exponente =){
    for (potencia == 0)
    (let iz = 0; i <= exponente; ii++);
        let resultado = calcularPotencia
    resultado *= base; 
    }
      return resultado = calcularPotencia {

      }
     console.log(base*exponente)
    }